// The shared GUI package contains no Go code. This module is a boundary that
// keeps gui/node_modules out of the root module's `./...`; see portal/go.mod
// for the full rationale.
module gui

go 1.26.6
