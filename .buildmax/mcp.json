{
  "mcpServers": {
    "local-test": {
      "type": "stdio",
      "command": "go",
      "args": ["run", "${WORKSPACE_ROOT}/cmd/local-test-mcp-server"],
      "env": {
        "MCP_DEMO_CUSTOM": "$BUILDMAX_MCP_CUSTOM"
      }
    }
  }
}
