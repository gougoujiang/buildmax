{
  "mcpServers": {
    "mcp": {
      "type": "stdio",
      "command": "go",
      "args": ["run", "${WORKSPACE_ROOT}/tools/mcp"],
      "env": {
        "MCP_DEMO_CUSTOM": "$BUILDMAX_MCP_CUSTOM"
      }
    }
  }
}
