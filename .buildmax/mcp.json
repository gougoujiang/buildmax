{
  "mcpServers": {
    "demo": {
      "type": "stdio",
      "command": "go",
      "args": ["run", "${BUILDMAX_WORKSPACE_ROOT}/cmd/mcp-echo-server"],
      "env": {
        "MCP_DEMO_CUSTOM": "$BUILDMAX_MCP_CUSTOM"
      }
    }
  }
}
