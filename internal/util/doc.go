// Package util contains small, cross-layer helpers with no business ownership.
//
// Keep package contents generic and dependency-light. Helpers that belong to a
// specific subsystem should live with that subsystem instead of being added here.
package util
