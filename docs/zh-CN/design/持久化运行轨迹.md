# 持久 Run 跟踪

> **翻译说明：** 本文是[英文原文](../../design/durable-run-trace.md)的简体中文派生翻译。**同步依据：** 英文原文 SHA-256 `349f82017d503039e5208aa1378ac2f33748f80f51512ad2919a47268e0673d5`。**同步状态：** 与该版本一致。若中英文存在语义冲突，以英文原文为准。


## 目录

- [状态](#状态)
- [1.用途](#1-目的)
- [2.方向](#2-方向)
- [3.存储型号](#3-存储模型)
- [4.分层](#4-分层)
- [5.运行时流程](#5-运行时流程)
- [6.配置](#6-配置)
- [7.超出本次通行证的范围](#7-超出此通道的范围)
- [8.实现步骤](#8-实现步骤)
- [9.接受](#9-验收)

## 状态

- roadmap_priority: `P0.5`
- 状态：`phase 1 implemented`（§8 第 1 阶段已落地；§7 的后续行动仍在开放中）
- 实现：[trust-harness.md](./信任保障.md) §3.3
- 如下：[hook-system.md](./Hook系统.md)
- 路线图：[../ROADMAP.md](../ROADMAP.md)
- created_at: `2026-06-03`

## 1. 目的

[trust-harness.md](./信任保障.md) §3.3 调用 **持久运行跟踪**
这解释了期间发生的事情
Agent运行：模型调用、工具调用、审批决策、hook执行、文件
更改、压缩、错误/重试、令牌使用和计时、子代理
关系、沙箱模式以及使用的内存/指令源。

共享 Agent 内核已发出结构化内存事件流
（`core/agent.Event`通过`RunLoopOpts.EventSink`），但没有任何东西可以坚持下去。当
运行结束后，唯一持久的工件是会话消息和旋转的
日志文件 - 两者都无法重建*Agent 做了什么以及为什么*。

该文档定义了一个建立在
现有事件流，附加在每个界面已使用的一个阻塞点
（`agentapp.RunPrompt`），所以CLI/TUI，Desktop，代理评估，和Worker全部
生成没有每个界面代码的痕迹。

这是§3.4（活动视图）、§3.7（子代理）的先决条件
可追溯性）、§3.8（工作人员诊断）和 Portal 运行诊断。

## 2. 方向

- **基于事件流构建，不要发明并行流。** 跟踪是一个
  `EventSink` 消费者。仅当事件流发生时才会添加新记录类型
  还不能表达某些东西（这个过程没有添加任何东西——它映射了现有的
  9 个 `EventKind` 加上合成的 `run_start`）。
- **一个阻塞点。** `agentapp.RunPrompt` 是 CLI 的单一调用站点
  （`print.go`、`tui_model.go`）、Desktop、评估和Worker
  （`agentapp/taskrun`）。记录仪的布线覆盖了每个界面。
- **始终打开失败。** 跟踪失败（磁盘已满、权限被拒绝、编码
  错误）绝不能中断或减慢运行速度。每个记录器错误都会在警告处记录
  并掉落。
- **默认情况下有界和编辑。**大型工具结果和模型内容
  被截断；在字节到达磁盘之前，常见的秘密形状会被擦除。
- **可检查，尚未成为产品。** 第 1 阶段持续存在 JSONL，该活动
  视图（§3.4）和未来的 `buildmax trace` 命令读取。此通行证中没有 UI。

## 3. 存储模型

### 3.1 布局

```
<BUILDMAX_HOME>/sessions/<session_id>/traces/<run_id>.jsonl
```

- 每次 `RunPrompt` 调用一个文件（一次运行）。
- 按会话分组，以便对话的运行坐在一起。
- 该记录首先将该目录放在`<BUILDMAX_HOME>/traces/<session_id>/`。
  [local-session-storage.md](./本地会话存储.md) §10 将其移至
  会话包；每次运行的合同不变，只是文件所在的位置不变。
  对于不属于任何会话的内容，痕迹根仍然存在 - 本地
  后台作业写入 `<BUILDMAX_HOME>/traces/jobs/<job_id>.jsonl`。
- `run_id` 使用前缀 ID 格式 (CLAUDE.md §6.3)，前缀为 `rt_`
  （“运行跟踪”）：`rt_<20 base36 chars>`。
- JSONL：每行一个自描述记录，仅附加。 JSONL 幸存下来
  运行中途崩溃（每条完成的行都是有效的）并且流传输成本低。
- 每条记录在写入时都会被刷新，直到关闭为止不会被缓冲。的
  上面的“survivs-a-crash”属性是格式的全部要点，并且
  缓冲尾部默默地将其带走：记录恰好在
  运行结果很糟糕，并且磁盘上留下的最后一个事件早于
  最后发生的事件 - 使读者诊断故障
  错误的地方。跟踪量是每个模型调用的少量记录，因此
  每条记录的写入成本并不值得为此付出代价。

### 3.2 记录模式

所有键均为 Snake_case (CLAUDE.md §6.1)。一个 Go 结构与 `omitempty`
字段和 `type` 鉴别器；与记录类型无关的字段是
省略。

```jsonc
// run_start — synthesized by the recorder at construction
{"ts":"2026-06-03T10:00:00Z","type":"run_start","run_id":"rt_…","session_id":"c_…",
 "workspace":"/path","model":"…","is_subagent":false,"trace_version":1}

// subagent run_start — parent_run_id is the immediate parent, so a trace tree
// can be reconstructed one edge at a time. Top-level runs omit the field.
{"ts":"…","type":"run_start","run_id":"rt_child","parent_run_id":"rt_parent",
 "session_id":"…","is_subagent":true,"trace_version":1}

// sandbox_boundary — synthesized at construction; added after phase 1 so a run
// states the boundary it ran under. Written even when nothing confined the run,
// because an absent record reads as "not checked" rather than "not confined".
{"ts":"…","type":"sandbox_boundary","sandboxed":false,"backend":"none",
 "sandbox_mode":"auto_allow","sources":["default:worker","policy"]}

// plugins — synthesized at construction, for the same reason: naming what the
// run loaded from outside the workspace, and for a repository plugin whether
// that input could still change under it. Bounded metadata only, never package
// content. See design/plugin-marketplace.md §10.
{"ts":"…","type":"plugins","plugins":[
  {"name":"code-review","source":"repository","remote_url":"git@…",
   "commit":"…","branch":"main","dirty":false},
  {"name":"release","source":"marketplace","version":"1.2.0",
   "digest":"sha256:…"}]}

// iter_start  (EventIterStart)
{"ts":"…","type":"iter_start","iter":1}

// llm_start   (EventLLMStart)
{"ts":"…","type":"llm_start","iter":1,"context_tokens":1234,"context_window":128000}

// llm_end     (EventLLMEnd) — content bounded
// The cache counts are a breakdown of prompt_tokens, not an addition to it, and
// are omitted entirely when the provider reported none.
//
// prompt_tokens and friends are the run's totals so far; the call_* fields are
// what this one call did. Both are here because subtracting consecutive
// records to get the second goes wrong as soon as a call in between failed.
// cost is this call's, in nano-currency-units, and is absent when the model
// was unpriced — which is not the same fact as a call that cost nothing.
{"ts":"…","type":"llm_end","iter":1,"has_tool_calls":true,
 "prompt_tokens":1200,"completion_tokens":80,
 "cache_read_tokens":900,"cache_write_tokens":100,
 "call_prompt_tokens":1200,"call_completion_tokens":80,
 "call_cache_read_tokens":900,"call_cache_write_tokens":0,
 "cost":{"currency":"USD","uncached":900000,"cache_read":270000,"cache_write":0,
         "output":1200000,"total":2370000,"baseline":4800000},
 "content":"…"}

// tool_start  (EventToolStart) — args bounded + redacted
{"ts":"…","type":"tool_start","tool":"writefile","tool_call_id":"call_1","args":"{…}"}

// tool_end    (EventToolEnd) — result bounded + redacted
{"ts":"…","type":"tool_end","tool":"writefile","tool_call_id":"call_1",
 "result":"ok","duration_ms":12}

// tool_denied (EventToolDenied)
{"ts":"…","type":"tool_denied","tool":"bash","deny_reason":"policy"}

// context_compacted (EventContextCompacted)
{"ts":"…","type":"context_compacted","summarized":3,"kept":5}

// user_input  (EventUserInput) — a message queued mid-run, joining at an
// iteration boundary; bounded + redacted, because it is part of what the run
// was told to do and a trace that omitted it would misreport its instructions.
{"ts":"…","type":"user_input","iter":3,"content":"also check the tests"}

// user_input_blocked (EventUserInputBlocked) — a UserPromptSubmit hook refused it
{"ts":"…","type":"user_input_blocked","iter":3,"content":"…","deny_reason":"no secrets"}

// run_end     (EventRunEnd)
// cost here is the run's, summed from the calls above. cost_incomplete says a
// call did work that could not be priced, so the total understates the run.
{"ts":"…","type":"run_end","tool_calls":2,"prompt_tokens":1200,
 "completion_tokens":80,"cache_read_tokens":900,"cache_write_tokens":100,
 "cost":{"currency":"USD","total":2370000,"baseline":4800000},
 "error":""}
```

`EventLLMDelta` **不**持久化 - 流增量是多余的
最终的 `llm_end` 内容会使文件膨胀。 `llm_end.content`搭载
有界的最终辅助文本。

### 3.3 边界

- `content`、`args`、`result` 均被截断为 `maxFieldBytes`（默认
  4096）带有 `… [truncated N bytes]` 后缀。剪切回退到 UTF-8
  符文边界，因此多字节字符永远不会被分割 - 部分符文会
  由 JSON 编码器重新编码为 U+FFFD。
- 每次运行记录上限（`maxRecords`，默认 10000）可防止失控循环；
  一旦命中，更多记录将被删除，并发出单个 `truncated` 警告
  已记录。始终尝试 `run_end` 记录。

### 3.4 修订

在写入之前应用于 `content`、`args`、`result`：

- `Bearer <token>` → `Bearer [redacted]`
- `sk-<≥16 chars>` → `[redacted]`
- `(?i)(authorization|api[_-]?key|token|secret|password)` 后跟 `=`/`:`
  和值 → 保留键，值 → `[redacted]`

编辑有意保守（基于关键字/形状）以避免损坏
正常输出。模式集被计划为录音机旁边的一个文件，
而是登陆`internal/util/secretscan`，因为作业日志和项目
记忆需要相同的模式；它仍然可以在不接触录音机的情况下生长，
现在也不触及其他两个调用者。

## 4. 分层

镜像 hook/MCP 布局（核心中的契约，基础设施中的实现，基础设施中的装配）
agentapp)：

```
internal/core/agent/event.go        # Event/EventKind — exists, unchanged
internal/config/trace.go            # TracesDir(), TraceEnabled(), env key
internal/infra/trace/record.go      # Record struct + FromEvent mapper + bounding
internal/util/secretscan/           # redaction patterns, shared with joblog and memory
internal/infra/trace/recorder.go    # Recorder: open file, Record(Event), Close()
internal/agentapp/app.go            # RunPrompt tees EventSink into a Recorder
```

导入合规性：`infra/trace` 导入 `core/agent` 和 `config`（两者）
允许用于 `infra`)； `agentapp` 已导入 `infra/*` 和 `config`。

## 5. 运行时流程

```
agentapp.RunPrompt(ctx, sess, prompt, stream, approval, eventSink)
  │
  ├─ resolveRunContext → model, client, session
  │
  ├─ trace.NewRecorder(TracesDir, meta{run_id, session_id, workspace, model})
  │     └─ writes run_start,           (nil + warn on error → fail open)
  │        then sandbox_boundary, context_sources, plugins
  │
  ├─ sink := tee(recorder.Record, eventSink)   // recorder first, then caller
  │
  ├─ agent.RunLoop(… EventSink: sink …)
  │     └─ emits iter/llm/tool/compaction/run_end → recorder + caller both see them
  │
  └─ recorder.Close()                  // flush + close file; no extra run_end
```

- 记录器是 T 恤的*第一*段，因此调用者的接收器中会出现恐慌
  不能丢失跟踪数据；双腿都尽力了。
- `RunResult` 获得 `TraceID string`，以便调用者可以将查看器指向该文件。
  它也填充在错误返回上（`RunLoop` 失败，`finalizeTurn`
  failure) — `RunLoop` 在返回之前发出 `run_end` 错误，因此
  跟踪已完成，并且运行失败正是调用者需要的时候。
- **提示被钩子阻塞**（`RunLoop`之前提前返回）：录音机
  写入 `run_start`，然后写入 `run_end{error:"blocked by hook: <reason>"}` 和
  关闭，因此受阻的转弯仍会留下一行解释。

## 6. 配置

- `config.TracesDir()` → `<DataDir>/traces`。
- `config.TraceEnabled()` → true 除非 `BUILDMAX_TRACE_DISABLED` 为真
  值（`1/true/yes/on`）。默认开启；注册在 `env_spec.go` 的 `EnvVars()` 库存中，
  记录在 `docs/reference/configuration.md` 中。
- Worker继承了相同的env，因此worker默认运行trace；他们的踪迹
  dir 是运行范围的 `global/` BUILDMAX_HOME，保存跟踪数据
  运行以供稍后上传/诊断。

## 7. 超出此通道的范围

- TUI/Desktop (§3.4) 中的活动视图 UI - 稍后读取这些文件。
- `buildmax trace` CLI 检查员/列表/导出。
- Hook-执行和批准-授予记录：仅`tool_denied`
  （原因=`hook`/`user`）从今天的事件流中可见。专用
  `hook_*` / `approval_*` 记录需要新事件并被推迟。
- 文件更改记录、沙箱决策记录、重试记录 — 推迟到
  事件流将它们呈现出来。
- 区分*为什么*运行失败。 `run_end.error`携带消息，但是
  由于依赖关系已关闭而失败的运行读取与
  模型或任务失败。 `/readyz` 回答依赖项现在是否已关闭；它
  无法回答为什么一次运行失败了。对故障进行分类需要类型化
  `run_end` 上的原因，事件流不携带该事件。
- 跟踪目录的保留/轮换/GC。 **接受后果
  明知：** 默认情况下跟踪处于打开状态，并且任何操作都不会删除跟踪，因此
  会话包无限增长——每次运行一个跟踪文件，每个跟踪文件都有上限
  10000 条记录 × ~4KB 的有界字段。本地使用不太可能被注意到；
  一个寿命长的工作容器，可以在运行期间保持其 `global/` 的位置
  真实曝光。在保留政策落地之前，运营商可以调整该数量
  来自每次运行上限或设置 `BUILDMAX_TRACE_DISABLED`。

## 8. 实现步骤

### 第 1 阶段（本次）

1. `config/trace.go`: `TracesDir()`, `TraceEnabled()`, `EnvKeyBuildmaxTraceDisabled`；注册到`env_spec.go`；将其记录在 `docs/reference/configuration.md` 中。
2. `util/secretscan` + 测试：关键字/承载/sk- 编辑。
3. `infra/trace/record.go` + 测试：`Record` 结构、`FromEvent`、边界。
4、`infra/trace/recorder.go`+测试：打开`<dir>/<session>/<run>.jsonl`下的文件，写入`run_start`、`Record(Event)`、记录cap、`Close()`；失败打开构造函数。
5. `agentapp/app.go`：在`RunPrompt`中建立记录器，tee the sink，将`TraceID`添加到`RunResult`，处理阻塞提示提前返回。
6. `agentapp/trace_wiring_test.go`：通过`RunPrompt`进行端到端接线测试 —
   已写入且可解析的跟踪文件（§9 项目符号 1），`BUILDMAX_TRACE_DISABLED`
   不写入任何内容（第 3 条），不可写的跟踪目录仍然可以完成运行
   （第 4 点）。他们驾驶着钩子阻挡的转弯，这是一条 `RunPrompt` 路径
   在没有现场 LLM 的情况下到达记录器。
7.`./make test`。
8. 文件：在已交付记录中标记 `trust-harness.md` §3.3 进行中/完成
   设置；更新`AGENTS.md`和`design/README.md`。

### 子代理联动

子代理跟踪记录其直接父代的 `parent_run_id`。这就形成了一个
跟踪树而不使子节点依赖于根运行标识符：以下
links 产生根，并且未来的嵌套子代理以相同的方式组成。当一个
父记录器无法启动，没有写入子跟踪，而是写入
误导未关联的孩子，保留故障开放行为。

### 后续（稍后通过）

- 活动视图读取跟踪（§3.4）。
- `buildmax trace` 检查员。
- Hook/批准/文件更改/沙箱记录（需要新事件）。
- 保留政策。

## 9. 验收

- 在任何界面上的任何 `RunPrompt` 之后，`<DataDir>/sessions/<session>/traces/<run>.jsonl`
  存在并包含 `run_start`、每次迭代 LLM/工具记录和
  终端 `run_end`。
- 超过界限的工具参数/结果和模型内容被截断；
  Bearer/`sk-`/关键字秘密已被编辑。
- 通过 `BUILDMAX_TRACE_DISABLED=1` 禁用不会产生任何文件，也不会出现错误。
- 强制记录器故障（不可写目录）记录警告并且运行仍然
  正常完成。
- 工作线程任务运行在运行范围的 `global/` 主目录中留下痕迹。
```
