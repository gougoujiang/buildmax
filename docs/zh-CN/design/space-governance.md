# Space 治理基础

> **翻译说明：** 本文是[英文原文](../../design/space-governance.md)的简体中文派生翻译。**同步依据：** 英文原文 SHA-256 `2eb2ae774877a3d2b8b51975133681b800120e3bc01b4de7a77338c469837aab`。**同步状态：** 与该版本一致。若中英文存在语义冲突，以英文原文为准。


## 内容

- [状态](#状态)
- [1. 决策](#1-决策)
- [2. 产品目标](#2-产品目标)
- [3. 当前基线](#3-当前基线)
- [4. 主要缺口](#4-主要缺口)
- [5. 范围内事项](#5-范围内事项)
- [6. 范围外事项](#6-范围外事项)
- [7. 权限矩阵](#7-权限矩阵)
- [8. 后端计划](#8-后端计划)
- [9. 前端计划](#9-前端计划)
- [10. 验证](#10-验证)
- [11. 风险](#11-风险)
- [12. 开放问题](#12-开放问题)
- [13. 建议的首个 PR](#13-建议的首个-pr)

## 状态

- roadmap_priority: `P4`
- status: `partially_implemented`——角色、配额、Workflow 生命周期、授权矩阵、
  审计轨迹及其保留窗口和导出、配额告警均已交付；第 4.4 节第二批动作和
  开放问题 7 的关联标识符仍待完成
- follows: [企业部署](./enterprise-deployment.md)
- roadmap: [路线图](../../ROADMAP.md)
- created_at: `2026-05-17`

## 1. 决策

P4 应把 BuildMax 已有的 Space 控制能力转化为适合私有 Space 运营的实用
治理基础。

已有基础包括：

- Space 角色：`owner`、`admin`、`member`
- Space 级配额服务
- Space 用量端点
- 集中的处理器授权辅助逻辑
- Workflow 生命周期：`draft`、`published`、`archived`
- Workflow 分配与执行检查

剩余工作不是打造庞大的企业策略平台，而是让现有控制足够可见、可测试、
可追踪，使 Space 管理员能够信任共享自动化。

## 2. 产品目标

管理员应能清楚了解：

- 谁能做什么
- 哪些共享资源受到治理
- Space 当前使用了多少容量
- 哪些 Workflow 处于草稿、已发布或已归档状态
- 敏感资产随时间发生了哪些变化

产品应建立这种信心，同时不迫使用户面对沉重的管理控制台。

## 3. 当前基线

后端锚点：

- `internal/core/space/space.go` 中的角色
- `internal/core/space/policy.go` 中的角色/动作决策，由
  `internal/server/access` 的 `Guard` 应用于请求
- `internal/service/quota/service.go` 中的配额服务
- `internal/server/handlers/space/usage.go` 中的配额路由
- `internal/core/workflow/workflow.go` 中的 Workflow 生命周期
- `internal/service/workflow/service.go` 中的 Workflow 生命周期执行规则
- `internal/infra/db/space.go` 中的 Space 持久化
- `internal/infra/db/quota_tier.go` 中的默认配额层级初始化

前端锚点：

- `portal/src/pages/settings/SpaceSettings.tsx` 中的 Space 设置
- `portal/src/pages/settings/shared.tsx` 中的共享设置 UI
- `portal/src/pages/workflows` 中的 Workflow 页面
- `portal/src/pages/issues` 中的 Issue 分配 UI

当前动作模型：

- `owner` 管理 Space 成员。
- `owner` 和 `admin` 管理 Agent、Workflow 和 Workflow 分配。
- `owner`、`admin` 和 `member` 均可运行 Workflow。

## 4. 主要缺口

### 4.1 治理不够清晰可见

后端已有角色和检查，但 UI 需要为管理员更清楚地说明：

- 每个角色的含义
- 为什么某项操作不可用
- 哪些 Workflow 状态可以运行
- 当前 Space 用量和配额限制

### 4.2 权限边界需要更广泛的测试——已解决

**这一缺口已经关闭。** 权限矩阵让 owner、admin、member、其他 Space 的
member 和匿名调用方分别通过真实 mux 发起请求，覆盖服务器注册的所有
Space 级路由。重点是驱动真实请求，而不是只对授权辅助函数做单元测试。
角色规则现在由 `core/space.Allows` 唯一实现并有表驱动测试；矩阵验证另一半：
每条路由是否真的执行了检查。第二项测试读取所有路由注册；Space 级路由若
没有矩阵条目会失败，矩阵若引用已不存在的路由也会失败，避免把失效条目误当覆盖。

该测试证明：

- member 不能修改共享自动化资产
- admin 不能执行涉及所有权的成员管理操作
- owner 可以管理成员
- Workflow 生命周期限制始终一致生效
- Space 级资源不会跨 Space 泄露

### 4.3 Workflow 生命周期需要产品打磨

生命周期存在，但在Portal中应该显而易见：

- `draft`：可编辑，但不能分配或运行共享工作
- `published`：可分配、可运行
- `archived`：保留历史，但不用于新工作

UI 不应让用户通过请求失败来摸索这些规则。

### 4.4 敏感资产不可追踪——部分解决

第 5.4 节描述的审计轨迹已经存在，但目前只覆盖清单中的身份与模型目录部分。

- Space 成员和角色——**已记录**
- 保存提供商凭证的模型目录——**已记录**
- Webhook 密钥——未记录
- Agent 定义——未记录
- Workflow（包括发布和归档）——未记录
- 配额层级分配——未记录

第一批覆盖项按资产受损的代价选择，而非按变更频率：成员变更会授予对
Space 全部内容的访问权，目录变更会改变提示流向和支出。其余项都是增量工作——
在变更点调用一次 `Record`，并增加永久动作字符串——自然构成第二批。

## 5. 范围内事项

### 5.1 Space 配额用户界面和文档

让管理员预期的配额可见：

- 现行空间使用
- 层级名称
- 滚动时间
- 运行限量
- 标志限制
- 过度限制行为

文件：

- 配额是空间范围的
- 个人使用由默认个人空间表示
- 任务创建/重运检查活动空间

### 5.2 角色和许可限制测试

添加服务器处理器和服务器的桌面驱动权限矩阵。

最低行动：

- 管理空间成员
- 创建/更新/删除代理
- 创建/更新/发布/存档工作流程
- 赋予工作流程问题
- 运行工作流程
- 阅读空间资源
- 创造正常的工作

### 5.3 Workflow生命周期UI

波兰工作流程列表/详细复制和控制：

- 显示生命周期标志
- 禁用清晰文本的不可用操作
- 隐藏存档工作流从默认分配选项中
- 保持存档工作流程可检查
- 让发布/存档过渡明确

### 5.4 小审计/事件模型  运输，第一片

相关组件是相关组件的相关组件 `audit.Event` `internal/core/audit/audit.go`

- **`ActorType`加上`ActorID`，而不是`ActorUserID`.** 工人和系统
打字演员比打字演员更便宜
发明一个用户身份证。
- **没有`MetadataJSON`.**一个自由形式的JSON列是提示，请求的地方
简单的简单，不敏感的注释 `Detail`
角色名字，模特名字， 没有什么。
- **可能是空的`SpaceID`.** 登录不具有空间范围，并且强迫一个
想创造一个场地来参加这个活动。

活动没有提示，没有生成的内容，没有工具输出，没有凭证：只有谁做了什么对什么对象。 Run诊断在`llm_call`账本中存在着持久的运行跟踪和每次通话会计，因为这些问题是不同的保留答案。

仅通过界面添加 没有更新或删除，因为可以编辑的记录不是证据。 动作字符串是持久的，因此是永久的；重新命名一个重写历史每一个读者过它。 `AuditStore`

发送的行动是`user.login`,`space.member_added`,`space.member_removed`,`llm_model.created`,`llm_model.enabled`,`llm_model.disabled`，和`access.denied`。 值得指出的是任何扩大列表的人：一个失败登录是故意 **未记录的，因为它没有说出演员是谁，并将轨迹转化为任意字符串的写作；而相关标识符是什么失败，因为否认是指标记一个边界。

错误的写作记录和丢弃，而不是失败的行动引发它.这是决定，不是差距：拒绝该行动将会将记录中断转化为记录中断的东西.这是一个真正的限制,`docs/start/support.md`表示它 追踪记录了数据库可访问时发生的事情，这不等于确保每一项行动都被记录。

其他4.4节的行动是第二部分。

### 5.5 事件可见性  发送

相关标识符是最新的空间轨道，首先具有限量/抵消，而Portal则作为空间设置中审计部分 (`portal/src/features/audit/`)。 `GET /api/spaces/{space_id}/audit-events`

仅供所有者使用**，在API和UI中。 轨道名称包括谁被拒绝，这是行政信息而不是协作信息。

### 5.6 保留  发货

在 `server.yaml` 中,`audit.retention_days` 过期事件比窗口更老。 它默认为 **0**，这保留了一切，因为一个从未选择过政策的部署没有决定丢弃证据。

扫描是BuildMax中唯一可以删除审计事件的东西，而狭窄的`AuditPruneStore`接口存在，使其保持这种方式：每一个读者和写作者都持有`AuditStore`，它们中的任何一个都无法达到删除。 没有什么可以删除一个 * 特殊的 * 记录。

每次删除任何东西都会写出一个名为范围和数量的`audit.pruned`事件。 这记录是删除完全可辩护的原因：一条开始的步骤必须说明政策是否缩短了它，或者有人缩小了它，而这些看起来是相同的.事件比产生它的截止更年轻，因此它存活着自己的扫描，在窗口通过它后的扫描说出了后的延伸。

这回答了第6个问题：保留是配置，默认保留，

### 5.7 出口  运输

系统管理员可以使用相同的过器来实现部署。 无论是流 CSV 或 JSONL。 `GET /api/spaces/{space_id}/audit-events/export` `GET /api/admin/audit-events/export`

值得遵守的三项决定：

- 开放问题8问出口需要
消费者会如何发现漏洞。
导航下载没有任何问题，然后把拉拉发送，意味着事件
任何人都可以获取数据之前，不必是最终的形状。
整合仍必须回答8问题，
- 航天路线不采用过器。
其他地方记录，在出路上应用过器是决定的文件
管理者路线确实带着它们，因为它们在那里
操作员将他们已经持有的读数缩小。
- **出口记录**，为`audit.exported`，实际计数为
读完整个记录是一个行动
只有一个不留痕迹的出口才能查看这条路径
管理器出口缩小到一个空间，
太空的痕迹也存在，所以它的所有者可以看到部署阅读了它。

页面化使用键盘设置缓冲器而不是偏移.出口在许多回路中读取，而一端添加了排行，在保留下，在另一端删除，并且在证据中，每次偏移都会被移除。

### 5.8 配额警报  发货

`QuotaService.Check`记录了两个操作：当空间超过80%的限制时， 相关标识符当工作被拒绝时.它们是不同的，因为它们要求不同的反应。 `quota.threshold_reached` `quota.exceeded`

每项限制在每个周期最多记录一次，并根据审计轨迹本身去重，避免持续提交工作的 Space 把自己的记录变成重试日志。行为主体是系统，而不是恰好让总量越过阈值的提交者：配额属于整个 Space，把最后提交的成员写成主体会被误读为对共享预算的个人归责。

录取路径是这个地方.使用是滚动窗口，因此没有时间限制，扫描可以注意到80%.读取或写入都不能改变录取决定。

根据Portal的使用数据，它在空间设置中表示相同的东西.这是快速的答案；

## 6. 范围外事项

- 定制角色。
- 政策 DSL
- 批准工作流程
- 根据每位代理或每次工作流的许可名单。
- 无变性合规档案。
- 推向外部水槽的审计交付。
推力必须回答开放的问题 8，但它没有。
- 收费。
- 组织层次。

## 7. 权限矩阵

建议的起始矩阵：

| 行动 | 业主 | 管理员 | 成员 |
|---|---:|---:|---:|
| 查看空间资源 | 没有 | 没有 | 没有 |
| 创建问题/对话工作 | 没有 | 没有 | 没有 |
| 分配工作流程Run | 没有 | 没有 | 没有 |
| 管理代理人 | 没有 | 没有 | 没有 |
| 管理工作流程 | 没有 | 没有 | 没有 |
| 将问题分配到工作流程中 | 没有 | 没有 | 没有 |
| 管理空间成员 | 没有 | 没有 | 没有 |
| 改变成员角色 | 没有 | 没有 | 没有 |
| 查看空间使用 | 没有 | 没有 | 没有 |
| 改变配额级别 | 没有 | 没有 | 没有 |
| 查看活动活动 | 没有 | 没有 | 没有 |

如果后来的企业客户需要更多的控制， 现在要从观察到的需求中建立起来， 而不是发明自定义的RBAC。

## 8. 后端计划

### 已完成的许可测试

由于第4.2节所述原因，该预言不能显示路线查询了路线.每个空间范围的路线服务器注册有一个列命名谁可以调用它，列被驱动为五个调用者，包括另一个空间成员的实际请求，而没有列的路线失败了构建。

接受性得到了满足：许可矩阵通过测试执行，而一个新的空间航线不能出发，

### M2。 管理服务界限

如果行动检查继续扩散，将政策转换为一个小服务/包。

目标API：

```go
type SpaceAuthorizer interface {
	Authorize(ctx context.Context, spaceID, userID string, action SpaceAction) (role string, err error)
}
```

让它无聊：没有保证DSL。

### 已完成 Space

运送为`audit.Event`和`audit.Store` (`RecordAuditEvent`/`ListAuditEvents`) 在`internal/core/audit`，并为[阅读中文镜像](space-governance.md)在单一表中运送.登录登录不是空间事件，因为登录不是空间测量，活动是证据而不是轨迹.参见图5.4页面的形状和从图片中丢弃的内容。 `auditEventRow` `internal/infra/db` `audit_event`

### 事件写作 完成

发生的事件发生在突变成功后.一个失败的写作被记录下来，因此管理记录从来没有失败它描述的行动。 交换是在5.4节和`docs/start/support.md`中声明的，而不是留给一个操作员在调查中发现。 紧的JSON元数据规则没有幸存下来：没有元数据列，只有一个短的`Detail`字符串。

### 事件已完成 API

作为：

```text
GET /api/spaces/{space_id}/audit-events
```

授权仅限所有者**，比这里描绘的所有者/管理员更窄，因为第5.5节的原因。 `{"events": [], "total": 0}`

## 9. 前端计划

### 复制和残疾国家的作用

更新Portal副本：

- 空间成员角色描述
- 禁用成员/管理员限制的操作文本
- 工作流程生命周期的解释

### 子代理 Space

在空间设置中，显示：

- 层次
- 时间
- 使用的运行
- 使用的代币
- 限制
- 没有限制的倒退，如果层次不清楚

### 子代理 Workflow

在工作流页面中：

- 标志状态
- 发布/存档活动为主/管理员
- 没有可用于草案/档案的运行行动
- 仅为已发布的工作流设设置的分配UI

### 活动部分 完成

作为"审计轨道" (`portal/src/features/audit/`) 的空间设置，具有简洁的标签和页面化.一个非所有者看到该部分解释为什么它为他们空，而不是看到什么，所以边界是可读的，而不是看起来像缺失的功能。

## 10. 验证

后端：

```sh
go test ./internal/server/handlers ./internal/service/quota ./internal/service/workflow ./internal/infra/db
```

前端：

```sh
cd portal && npm run build
```

完整：

```sh
./make test
```

手动情况：

1. 拥有者可以增加/移除成员。
2. 管理员可以管理工作流程，但不能管理成员。
3. 成员不能管理工作流程或代理人。
4. 发布的工作流可以分配和运行。
5. 对于新工作，不能分配草案/存档工作流程。
6. 相关标识符使用可见，并与活跃空间相匹配。 Space
7. 敏感行动在Space活动中出现。

## 11. 风险

- 过于早的治理**：避免关税角色和批准
基本的可追溯性土地。
- **审计噪音**：在第一片只记录有意义的敏感行动。
- **活动/事件不匹配**：保持事件行动名称稳定和记录。
- **UI混乱**：使管理在设置和工作流页面中可见，而不是
在任何地方。
- ** 默默事件失败**：记录事件写错误有足够的背景。

## 12. 开放问题

1. 成员应该能够查看Space活动，还是仅供管理员使用？
只有所有者**，比任何选择都更窄。
拒绝了申请，这是行政信息见5.5。
2. 事件写作应该是最好的努力还是需要敏感的行动？
**决定：最好的努力**,`internal/service/audit`的原因是：
拒绝登录，因为一个失败的审计插入将登录中断变成
认证中断。 错误记录错误的写作
现在，我们在这个地方做了一些事情，
任何一个行动是否值得一个
必须在同一个交易中写入的成功记录与该交易所采取的行动
描述.[系统管理.md](./system-administration.md) §9 论
由于补助措施是最弱的，
调查最需要的，是被记录的。
回答这个意思是决定呼叫者看到什么当行动成功
报告没有。
3. 工作流发布/存档是否需要主持人或允许管理员？
4. 在P4中应实施的配额级别变化还是仅需记录？
5. 网络关键的创建/撤销是否仅需要所有者/管理员？

其他问题来自已退休的"审计和数据治理"提案。 它的建议方向是：

6. 审计事件的保留是什么，它是配置还是
运营责任?~~ **决定：配置，默认保留
`audit.retention_days`，应用于部署而不是
通过一个空间，每次扫描记录了它所删除的内容。
7. 相关性识别器可以连接任务，工作者，模型调用和
部分答案：一个跑步的痕迹和它产生的`llm_call`行
现在加入了Portal的运行细节，所以运行所做的和
审计轨迹仍然没有被审计。
没有审计行动今天的运行
开始于审计活动时仍然无法机械地达到
造成了。
8. 产品出口需要至少一次交付，
部分部分的出口，
问题是未来任何推动一体化，
写出最好的努力 (问题2) 仍然意味着差距并不总是
没有发生过任何事。
9. 谁可以读取运行痕迹，Artifact和模型使用？
答案是解决的；这三个人从来没有一起决定，
一个痕迹保留工具输出。
10. 空间得到哪些删除控制？
命令和什么都会删除一个空间的记录，所以"删除我们的数据"没有
答案不仅仅是放弃数据库和桶。

## 13. 建议的首个 PR

第一个P4公关应该明确现有治理：

1. 添加许可矩阵测试。
2. 波兰空间设置角色/配额UI复制。
3. 波兰工作流程生命周期 UI状态。
4. 增加未能找到禁止角色路径的操作测试。

然后第二个PR可以添加小的`space_event`模型和Space活动UI。
