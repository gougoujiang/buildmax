# Portal 执行模型：Tier 1、Tier 2 与结果交付

> **翻译说明：** 本文是[英文原文](../../design/portal-execution-model.md)的简体中文派生翻译。若中英文存在语义冲突，以英文原文为准。


## 内容

- [状态](#状态)
- [1. 决策](#1-决策)
- [2. Tier 1 与 Tier 2 的区别](#2-tier-1-与-tier-2-的区别)
- [3. 转录与执行事件保持分离](#3-转录与执行事件保持分离)
- [4. 交付内容](#4-交付内容)
- [5. 延后的内容及原因](#5-延后的内容及原因)
- [6. 被否决的模型](#6-被否决的模型)
- [7. 被否决的方案](#7-被否决的方案)
- [8. 需要证据的问题](#8-需要证据的问题)

## 状态

- roadmap_priority: `P2 follow-on`
- status: `superseded`——第 5.1 节的目标（Space 拥有 Task，`conversation_id`
  可选）已经交付；第 4.2 节中要求的结果交付机制也已经交付。本记录关于
  Conversation 拥有 Task 的形态已被[Agent 执行与 Task 线程](Agent执行与Task线程.md)
  取代，当前执行模式以那篇记录为准；本文仍保留 Tier 1/Tier 2 边界的推理。
- follows: [产品愿景](./产品愿景.md)、[界面定位](./界面定位.md)
  和[Agent 执行与 Task 线程](./Agent执行与Task线程.md)
- roadmap: [路线图](../ROADMAP.md)
- created_at: `2026-08-23`

本文于 2026-08-22 作为提案提出并被接受，之后被本记录取代。结果投影的决定
仍然有效，但第 4.2 节的交付决定已被后来的实现取代：Conversation 是可选的
前台起点；Space 直接拥有 Task 和 TaskRun，Agent 可以在没有 Conversation 的
情况下执行。

当前方案见[数据模型](../contribute/architecture/data-model.md)，请求路径见
[Server 架构](../contribute/architecture/server.md)；本文保留相关推理。

## 1. 决策

本节描述曾经实现过、并推动结果交付工作的模型。对于新的执行路径，后来的
[Agent 执行与 Task 线程](Agent执行与Task线程.md)是权威记录：Tier 1
是可选的，直接运行 Agent 会创建 Task 和 TaskRun，无需 Conversation，结果呈现
也不需要 Tier 1 合成。

之所以区分这些层，是因为它们承担的职责不同：

1. **Tier 1：前台 Agent。** 负责与用户互动，理解意图，处理前台工作，分解任务，
   选择执行器，发起执行并合成结果。
2. **Tier 2：执行 Agent 平面。** 专注于后台工作，允许执行在没有 Portal 前台
   运行时的情况下继续。
3. **持久执行基础设施。** Task、TaskRun、调度器和 Worker 保存状态、输出、Artifact、
   追踪和用量；它们不是另一类 Agent。
4. **结果投影。** Conversation 任务卡、发布结果和通知都来自持久状态，而不是
   来自仍然打开的浏览器连接或一次额外的总结调用。

许多错误来自于一个在公告时制造的投影，而不是从记录中提取。

## 2. Tier 1 与 Tier 2 的区别

区别不在模型大小或推理强度，而在执行关系：

| 维度 | Tier 1 前台 | Tier 2 后台 |
|---|---|---|
| 用户关系 | 当前交互中的回答 | 继续运行时不要求用户在线 |
| 时间 | 预期为较短延迟 | 可能持续更久 |
| 持久性 | 不要求独立重试或取消 | 必须持久、可重试、可取消 |
| 隔离 | 轻量共享前台策略 | 自己的 Worker、工作区和资源策略 |
| 专业化 | 通用互动与协调 | 特定工具、模型、记忆或 Agent |
| 规模 | 一轮中的少量动作 | 多个可独立运行的任务 |

把工作交给 Tier 2 并不意味着 Tier 1 无法完成它，而是说明该工作的执行属性
不适合前台交互循环。

Tier 1 不应承担以下职责：保证持久交付；把模型记忆当作任务状态来源；在没有
结构化状态时猜测任务是否完成；把 Worker 输出重新标记成用户消息；成为从 Issue、
Workflow 或 webhook 创建任务的必经路径；或通过自己的总结判断结果卡是否存在。

## 3. 转录与执行事件保持分离

用户消息和 Agent 答案属于转录；任务卡是结构化的执行投影。任务卡不伪装成消息，
也不会把执行状态合成为一条消息。

界面按 `created_at` 合并显示这些内容；如果消息和任务在同一秒创建，则稳定的
类型优先级保证顺序，因为运行时先读取消息再启动任务。只有未来确实需要严格的
总序时，才引入会话序列；首版任务卡不证明需要事件存储。

内容类型依据信任水平保持分别：

| 内容 | 生产商 | 使用者说明 | 在LLM历史中默认 |
|---|---|---:|---:|
| 用户信息 | 用户 | 现在 | 现在 |
| 规范化说明 | 级 1，服务器验证 | 产品 | 只有目标工作者运行 |
| 输出 Worker | 级2代理和工具 | 没有 | 否；主持人不值得信赖的数据 |
| Task状态，使用，追踪检查 | Runtime | 没有 | 通过结构化工具 |
| 演讲者总结 | 演讲者模型 | 没有 | 可能继续作为助理答案 |

Worker 输出永远不会以 `role=user` 重播。总结模型也不能在没有独立授权的编排操作
下启动或继续任务。

## 4. 交付内容

### 4.1 结果投影来自持久状态，而不是通知声明

一个对话每次开始的任务都带有一个卡，从任务路线中读取，并在每次无效时重新加载 Socket 报告.状态，输出，文件，运行细节，停止，然后再运行.卡存活更新，丢弃的 Socket，总结从来没有到达。

终端运行将`task.status.changed`发射到空间上的每个连接一个无效，而不是结果.它用来选择创作者的第一个插座，它没有任何消息，并且在有三个时告诉一个页。

转录不包括系统频道，因此`[Task Result]`消息不再被用户绘制为自己的。

### 4.2 交付持续  超额

本子部分描述了原始机制：每次运行一个行 (`task_result_delivery`)，声称两个服务器不能两次运行一次，再试一次扫描，在有限数量的尝试后被遗弃，理由保持，呈现文本取自每次运行的尝试而不是存储。

终端运行现在只播出无效事件 (`task.status.changed`)；没有任何东西会在一个结果持久或可见之前，要求进行演示尝试，重新尝试或需要前景模型调用.本次部分的边界现在是结构性的，而不是重新尝试的政策： **TaskRun结果是持久的，没有句子关于它**，因为Conversation任务卡直接读取[阅读中文镜像](Portal执行模型.md)，并且没有任何代码需要写过。 `task_run` [执行代理和Task线程 §4](Agent执行与Task线程.md#4-支持的输入路径)

### 4.3 意图与执行有着可追溯的联系

运行中每次运行记录了自己的：任务的第一个运行命名为创建它的消息，一个延续命名为要求它的消息.它是按轮而不是通过工具调用，所以模型不能选择或省略其工作归因。 `task_run.source_message_id`

工人接下来给了指示,`GET /api/spaces/{space_id}/task-runs/{task_run_id}`引用了该消息.它们是不同的文本，这就是这个问题：该指示中缺少的限制是模型丢弃的或用户从来没有给出的，其他任何东西都不能区分它们。

### 4.4 哪个定义被记录

编辑一个代理变化了其下一个运行所设定的，并且该数值是必要的原因：一个任务的两个运行可以执行不同的文本.第一份写得胜，因此在运行期间的编辑不能重写该运行所给的内容。 `task_run.agent_revision`

**选择源不需要列.**每一个选择代理的网站都与一个不同的`trigger_source`对对一：一级1的`StartTask`是`portal_conversation`，任务路线是`portal_task_create`，一个问题运行是`issue_agent_run`，一个工作流程步骤是`workflow_step`.第二个记录的相同事实只会让它漂移。

### 4.5 合成Conversations已经消失了，不仅隐藏了

工作流动步骤和一个问题代理运行用于每一个创建对话，因为Task需要一个，由一个道过器隐藏于`ListConversationsBySpace`。 既不会再创建一个：两者都创建了一个Space-拥有的Task直接 (§5.1)。 `workflow` `issue_agent` `SyntheticChannels()`

## 5. 什么是延迟，为什么

### Conversation

这是在这里被推迟，因为它不是一个零的列变化：它需要在工人目录和对象存储密钥中解决`ConversationID`，通过Conversation授权的Artifact处理器，会议恢复，以及API路径，假设Conversation。 这种切除已经在一个变化中运输，因为推迟说明说它必须:`task.space_id`是必要的，并且授权所有权,相关标识符是一个零的关系，存储程序由空间/任务处理器而不是相关标识符 `task.conversation_id` Task TaskRun Artifact [执行代理和Task线程 §13.1](Agent执行与Task线程.md#131-所有权割接)

### 5.2 第二级目录

代理定义应该包含哪些功能，工具，输入/输出合同和执行配置文件领域是**证据关闭**.现有定义和使用应该首先确定哪些类别是稳定的.现在写这些领域将是发明需求。

在证据存在之前，不要预先定义`ResearchAgent`,`CodingAgent`或类似的Go类型。

已经运行的:Tier 1通过`StartTask`,Workflow,Issue，并明确地指定每个点的任务路线，通过空间总结推一个代理.Space会员身份已验证；模型和执行政策没有。

### 5.3 资产阶级前景预算和执行情况

预先时间，工具，许可和资源预算，可能会读到的 Tier 1 文本，以及验证的 ExecutionSpec 都开放。

由于Tier1已经在实践中被限制了：十次代，一个非互动政策，四个任务工具，没有文件系统或访问。

它们的杆是`source_message_id`，现在它已经存在了。

### 5.4 执行二级期间结构性总结

通过在员工运行中生成总结，可以节省一个模型调用。 它仍然是一个评估而不是一个决定：没有人把这个调用作为成本。

### 5.5 产品语言

定义一级和二级为以生命周期分离的前景和背景层次，并描述Task,TaskRun，规划器和工人作为基础设施而不是另一种代理类型，就是命名没有行为附加的工作.它属于5.1节后，这改变了这些对象的内容。

## 6. 不建议的模型

- 复制完整输出，每次运行终端输出时将其复制成`run_outcome`行；
- 交谈级任务状态与TaskRun状态分开；
- 编码任务卡作为一个Markdown助理信息；
- 每个Portal对象的一个普通多形事件表；
- 它们将每一个任务都自动推广到一个问题。

## 7. 拒绝其他选择

| 其他方法 | 为什么不 |
|---|---|
| 保持原始的1级 →2级路径不变 | 无线回复丢失，无法追踪的重写，强制性额外的模特调用，合成对话 |
| 融合成一个同步的代理 | 长时间工作阻止对话；浏览器的使用寿命泄露；持续的重试，取消和隔离丢失 |
| 暂停和恢复单个代理运行 | 需要持续的延续，检查点和跨过程流媒体远远超出了这个问题。 |

## 8. 需要证据的问题

- **第1级应具有哪些前景功能.**
期待在谈话中得到答案，而不是被发送。
- **是否已经存在稳定的二级专业代理商.**使用应说明
其他类别，而不是本文。
- **用户是否想要自动总结**，或者更喜欢卡和
根据要求，解释。 这决定了4.2的价值。
- **Conversation和Issue** 在聊天中
要求应该成为追踪工作。
- **每次对话是否需要严格的时间表序列**，
现在，我们已经做了很多事情。 `created_at`
