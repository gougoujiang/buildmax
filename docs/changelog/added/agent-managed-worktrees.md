- Ask the TUI agent for a worktree and it makes one, moves the session into it,
  and works there — every tool follows, with no path prefix. `/worktree` shows
  what exists and who is in it; removal asks first and refuses to discard
  uncommitted work.
