- The Portal task page now shows each run as its own card with status,
  trigger, duration, the agent revision that ran, and per-run Trace and Files
  buttons, plus a details panel (agent, trigger, timing, run count) — instead
  of a flat chat transcript with only a title and Retry.
