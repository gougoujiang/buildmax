# Smell Proposal — 2025-03-01 (internal/: Duplication & Unused/Temp Code)

## Scope

**Analyzed**

- Paths / packages: `internal/`
- Focus: (1) Duplicated code — how to refactor, including extraction to new packages for reuse and clear dependency; (2) Unused or temporary code — identify and propose cleanup.

## Summary

- **Proposals**: 10 (7 duplication, 3 unused/cleanup)
- **High priority**: 3 (argument parsing consolidation, artifact path validation, worker API helper)
- **Overview**: Duplication appears in path validation, rel-path normalization, worker HTTP client setup, argument parsing (tools vs util), and server limit/offset parsing. One unused exported helper (`OptionalString`) and optional test-helper dedup are recommended for cleanup. No FIXME/HACK/temporary or dead branches found.

---

## Proposals

### 1. Unify path validation in artifact content handler with `blob.CleanRelPath`

**Location**: `internal/server/artifacts.go` (lines 138–141)

**Current state**

- The handler implements its own path check: `strings.Contains(pathParam, "..") || filepath.Clean(pathParam) != pathParam || strings.HasPrefix(pathParam, "/")`.
- `internal/infra/objectstore/relpath.go` already provides `CleanRelPath(p)` which normalizes and validates relative paths (no `..`, no leading `/`, backslash normalization) and returns a clear `ErrInvalidPath`.
- Two different validation styles for the same idea (no traversal, no absolute) increase maintenance and risk of drift.

**Proposed change**

- In the artifact content handler, replace the inline condition with: call `blob.CleanRelPath(pathParam)`; if it returns an error, respond with `http.StatusBadRequest, "invalid path"`.
- Dependency stays server → blob (already present). No new package.

**Benefit**: Single source of truth for “safe relative path”; consistent behavior and errors; less duplicated logic.

**Priority**: High

---

### 2. Reuse rel-path normalization in S3 list (blob package)

**Location**: `internal/infra/objectstore/s3_persist.go` (line 55)

**Current state**

- After `TrimPrefix`, the code does `rel = path.Clean(strings.ReplaceAll(rel, "\\", "/"))` and then skips `""` or `"."`.
- `relpath.go`’s `CleanRelPath` does the same normalize (Clean + ReplaceAll backslash) and validation. Here we only need normalization (the key segment is already under a prefix, so `..` is less of a concern but normalization is duplicated).

**Proposed change**

- Option A: Add an internal helper in blob, e.g. `normalizeRelSegment(s string) string` that does `path.Clean(strings.ReplaceAll(s, "\\", "/"))`, and use it in both `CleanRelPath` (internally) and in `s3_persist.go` when building the list. Option B: After TrimPrefix, call `CleanRelPath(rel)` and ignore the error by skipping invalid entries (treat invalid as skip). Prefer Option A to keep “normalize only” separate from “validate and return error.”
- Keep everything inside `internal/infra/objectstore`; no new package.

**Benefit**: One place for “normalize path segment”; fewer copy-paste bugs if logic changes.

**Priority**: Medium

---

### 3. Extract worker API HTTP helper in executor

**Location**: `internal/execution/worker_api.go` — `GetWorkerChatRun` (lines 35–56), `WorkerHTTPUpdater.UpdateRunStatus` (lines 95–118), `WorkerHTTPStreamSender.SendDelta` (similar pattern)

**Current state**

- Each place builds URL from BaseURL + path, sets `Authorization: Bearer <token>`, uses `cfg.Client` or `http.DefaultClient`, calls `Do`, then checks status. Same pattern repeated three times.

**Proposed change**

- Add a package-level helper in executor, e.g. `workerDo(ctx context.Context, cfg WorkerAPIClientConfig, method, pathSuffix string, body []byte) (*http.Response, error)` that: builds full URL, creates request, sets Bearer if token non-empty, uses cfg.Client or DefaultClient, runs Do, returns response (caller handles status and body).
- Use this helper in `GetWorkerChatRun`, `WorkerHTTPUpdater.UpdateRunStatus`, and `WorkerHTTPStreamSender.SendDelta`.
- No new package; keep in `internal/execution` to preserve clear dependency (executor → workerapi, entity).

**Benefit**: Single place for URL building, auth, and client default; easier to add timeouts or retries later.

**Priority**: High

---

### 4. Standardize argument parsing inside `internal/execution/agenttool`

**Location**: `internal/execution/agenttool/argparse.go`

**Current state**

- Argument parsing is tool-specific and now lives next to the agent tools as private helpers: `parseRequiredString`, `parseOptionalString`, `parseOptionalInt`, `parseOptionalBool`, and `toFloat64`.
- `internal/util/argparse.go` and the old exported tool arg helpers are gone, so there is no cross-package helper surface for tool argument parsing.

**Proposed change**

- Done. Keep these helpers private to `agenttool` unless another package needs the same contract.

**Benefit**: One place for argument parsing; clear dependency; consistent behavior and errors across all tools.

**Priority**: High

---

### 5. Add server helper for limit/offset query parsing

**Location**: `internal/server/chats.go` (lines 107–116) and `internal/server/conversation_handlers.go` (lines 84–94)

**Current state**

- Both parse `limit` (default 50, with max 200 vs 100) and `offset` (default 0, ≥ 0) from query with the same pattern: strconv.Atoi, bounds check, default.

**Proposed change**

- Add an internal helper in server, e.g. `parseLimitOffset(q url.Values, limitKey, offsetKey string, defaultLimit, maxLimit int) (limit, offset int)`, and use it in both handlers with the appropriate max (200 for chats, 100 for conversations).
- Keep in `internal/server`; no new package.

**Benefit**: One place for pagination query parsing; consistent defaults and bounds; less duplication.

**Priority**: Medium

---

### 6. Unify test pointer helpers (optional)

**Location**: `internal/server/chats_test.go` — `ptrStr`; `internal/infra/db/store_test.go` — `ptrString`

**Current state**

- Same helper (`func X(s string) *string { return &s }`) defined in two test files.

**Proposed change**

- Either: (A) introduce a small test helper package (e.g. `internal/mock`) with `func PtrString(s string) *string` and use it from both tests, or (B) keep both if you prefer no cross-package test deps. Prefer (A) only if you add more shared test helpers later.
- If extracting: new package `internal/mock` (or `internal/server/mock` if you want to avoid entity depending on a shared mock). Document that it is for tests only.

**Benefit**: Single definition; optional and low impact.

**Priority**: Low

---

### 7. Consider shared path/validation package only if more callers appear

**Location**: `internal/util/workspace.go` (ResolvePath — root-scoped resolution + “path outside allowed root”) vs `internal/infra/objectstore/relpath.go` (CleanRelPath — relative-path normalization and validation)

**Current state**

- ResolvePath is for tool use (resolve under a workspace root). CleanRelPath is for storage keys and server path params. Different semantics; not wrong to have both.
- If more handlers or packages need “safe path from user input,” they could all use `blob.CleanRelPath` (server and blob already do). No need to merge ResolvePath and CleanRelPath into one package unless a third “path” abstraction emerges.

**Proposed change**

- No change now. If you later add more path-validation call sites outside blob, consider a small `internal/pathutil` that wraps or re-exports a single “safe relative path” API and is used by server and blob to avoid server → blob only for path logic. For current scope, reusing `blob.CleanRelPath` in the artifact handler (Proposal 1) is enough.

**Benefit**: Clear dependency; avoid over-abstracting until needed.

**Priority**: Low

---

## Unused and temporary code

### 8. Remove unused exported tool argument helpers

**Location**: `internal/execution/agenttool/argparse.go`

**Current state**

- Done. Argument helpers are private to `agenttool`; there is no exported `OptionalString` helper.

**Proposed change**

- No further action.

**Benefit**: Less dead code; clearer public API.

**Priority**: Medium (do together with Proposal 4)

---

### 9. No FIXME/HACK/temporary or dead branches

**Location**: `internal/` (full tree)

**Current state**

- Grep did not find FIXME, HACK, “temporary,” or “debug print” in internal. No `if false` or obvious unreachable branches.

**Proposed change**

- None. No cleanup required for this category.

---

### 10. Test-only ptrStr/ptrString — keep or unify

**Location**: `internal/server/chats_test.go`, `internal/infra/db/store_test.go`

**Current state**

- Two identical helpers; only used in tests.

**Proposed change**

- Keep as-is, or unify via a shared test helper (see Proposal 6). No functional bug; optional cleanup.

**Benefit**: Slight dedup if you introduce mock; otherwise leave as-is.

**Priority**: Low

---

## Summary table (duplication)

| # | Location | What’s duplicated | Refactor / extract to |
|---|----------|--------------------|------------------------|
| 1 | server/artifacts.go vs blob/relpath | Path traversal check for query param | Use `blob.CleanRelPath(pathParam)` in handler |
| 2 | blob/relpath.go vs s3_persist.go | path.Clean + ReplaceAll backslash | Same package: shared normalizer in blob |
| 3 | executor/worker_api.go (3 call sites) | BaseURL + Token + Client + Bearer + Do | Internal helper `workerDo` in executor |
| 4 | util/argparse vs tools/args | Required/optional string and int from map[string]any | Standardize on util; tools call util only |
| 5 | server/chats.go vs conversation_handlers.go | limit/offset query parsing | Server helper e.g. parseLimitOffset |
| 6 | server + entity tests | ptrStr / ptrString | Optional: shared mock |
| 7 | util/workspace vs blob/relpath | Different path semantics | No new package now; reuse blob where applicable |

## Summary table (unused / temp)

| # | Item | File / symbol | Suggestion | Reason |
|---|------|----------------|------------|--------|
| 8 | OptionalString | tools/args.go | Remove | Exported and never used; util covers it |
| 9 | FIXME/HACK/temp | internal/ | None | None found |
| 10 | ptrStr / ptrString | chats_test.go, store_test.go | Keep or unify in mock | Test-only; optional dedup |

---

*Proposal only; no code changes. Implement in separate tasks/PRs as needed.*
