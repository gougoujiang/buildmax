# Task - 001: API integration

CLI:
- Add help flag
- Add `-p` flag: user can run e.g. `buildmax -p "what can you do"`; the system calls the LLM, gets the response, and returns it to the user (simplest chat scenario)

API integration:
- Integrate OpenAI client library and call OpenRouter’s OpenAI-compatible endpoint
